[pytest]
# The rules keep their tests inside the rule module, behind
# `if "pytest" in sys.modules:` — the upstream ansible-lint idiom — so pytest's
# default `test_*.py` discovery misses them. Treat every module at the repo root
# as a test module so the embedded tests are collected.
testpaths = .
python_files = *.py
